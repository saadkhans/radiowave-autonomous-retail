"""Thin local HTTP bridge for the Radiowave Observatory.

The Python Foundation stays authoritative: this package only exposes the existing
simulator, pipeline and replay machinery as explicit view-model contracts over
HTTP. No database, authentication, broker or vendor SDK lives here.
"""
